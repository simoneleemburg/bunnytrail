# Drop world-wide CSS (SVG figure chrome, inline-svg classes that the

# world's bake scripts emit, etc.) into `inline-svg.css` here. It's

# served at `/api/assets/inline-svg.css` and referenced from app.html.
