singular: Settlement
plural: Settlements
# Settlements *live in* regions structurally (Bal Rochan sits inside
# Nuunlau) but aren't a *kind of* region — a city isn't "a region that
# happens to be small". Parenting to `place` keeps settlements as their
# own branch of the kind tree, sibling to region rather than under it.
# TODO(worldbuilding): author a one-line description for the
# settlements kind page.
